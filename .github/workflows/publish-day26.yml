name: Day 26 publication — retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Day 26 is frozen
        run: |
          echo "Day 26 and Chapter III are complete and frozen."
          echo "Do not reuse this workflow for future daily publications."
          echo "Future days must use a dedicated protected publication path."
