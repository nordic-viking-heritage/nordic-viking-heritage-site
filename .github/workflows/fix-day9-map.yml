name: Fix Day 9 map asset

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fix-day9-map.yml'
      - 'map-chunks/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  decode-map:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Decode Day 9 map
        run: |
          cat map-chunks/day9-01.txt map-chunks/day9-02.txt map-chunks/day9-03.txt map-chunks/day9-04.txt > day9-map.b64
          base64 --decode day9-map.b64 > master-map-day-9.jpg
          file master-map-day-9.jpg
          test -s master-map-day-9.jpg
      - name: Commit corrected map
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add master-map-day-9.jpg
          if git diff --cached --quiet; then
            echo "Map already correct."
          else
            git commit -m "Fix Day 9 master map image"
            git push origin main
          fi
