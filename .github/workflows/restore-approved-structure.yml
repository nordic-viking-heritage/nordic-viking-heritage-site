name: Legacy structure restore — retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic legacy restore is retired to prevent stale content from overwriting the current site."
          echo "Use validation and an explicit reviewed repair instead."
