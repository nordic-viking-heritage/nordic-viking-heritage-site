name: Validate permanent site structure

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - index.html
      - styles.css
      - 'archive/**'
      - 'api/**'
      - 'scripts/**'
      - '.github/workflows/validate-site-structure.yml'
  pull_request:
    branches:
      - main
    paths:
      - index.html
      - styles.css
      - 'archive/**'
      - 'api/**'
      - 'scripts/**'
      - '.github/workflows/validate-site-structure.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate Day 26 locked website baseline
        run: python3 scripts/validate_site_structure.py
